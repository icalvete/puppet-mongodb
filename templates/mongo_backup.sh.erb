#!/bin/bash

BACKUP_DIR=<%= @backup_dir -%>/mongo
DATE=`/bin/date +%Y%m%d%H%M`

cd $BACKUP_DIR
/usr/bin/mongodump
mv dump $DATE

for a in `$BACKUP_DIR`; do
	/usr/bin/find /srv/backup/mongo -maxdepth 1 -mindepth 1 -type d -mtime +3 -exec rm -fR {} \;
done

