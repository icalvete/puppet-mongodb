#!/bin/bash

BACKUP_DIR=<%= @backup_dir -%>/mongo
DATE=`/bin/date +%Y%m%d%H%M`

cd $BACKUP_DIR
/usr/bin/mongodump
mv dump $DATE

/usr/bin/find /srv/backup/mongo -maxdepth 1 -mindepth 1 -type d -mtime +<%= @backup_retention -%> -exec rm -fR {} \;
